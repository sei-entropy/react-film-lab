import React from "react";
import TMDB from "./TMDB";
export default class Filmlisting extends React.Component {
  render() {
    // const Movie = TMDB.films;
    // const filmsTitle=Movie.map(function(title,index){
    // return <li key={index}>{title =Movie[index].title}</li>
    // });

    return (
      <div>
        {/* <h1>{TMDB.films[0].title}</h1> */}
        {/* <ul>
        {filmsTitle}
    </ul> */}
      </div>
    );
  }
}
